import type { Hexagram, LiuYaoPanel, Bazi } from '../types';
import { wuXingOf } from '../utils/parser';
import { getHexagramByIndex, findHexagramByTrigrams } from '../data/hexagrams';
const LIU_SHEN = ['青龙','朱雀','勾陈','螣蛇','白虎','玄武'];
function makeHex(index: number): Hexagram { const r = getHexagramByIndex(index); return { index, name: r.name, fullName: r.fullName, upper: r.upper, lower: r.lower, palace: r.palace, element: r.element, nature: r.nature, lines: [], shiPosition: r.shiPosition, yingPosition: r.yingPosition }; }
export function buildLiuYao(ben: Hexagram, dong: readonly number[], bazi: Bazi): LiuYaoPanel { const raw = getHexagramByIndex(ben.index); const changed = raw.lines.map((b, i) => dong.includes(i + 1) ? !b : b); const bitsToIdx = (a: readonly boolean[]) => (a[0] ? 4 : 0) + (a[1] ? 2 : 0) + (a[2] ? 1 : 0); const bianIdx = findHexagramByTrigrams(bitsToIdx([changed[0], changed[1], changed[2]]), bitsToIdx([changed[3], changed[4], changed[5]])); const palaceIdx = ['乾','坎','艮','震','巽','离','坤','兑'].indexOf(ben.palace); const fuShi = Array.from({ length: 6 }, (_, i) => ({ hex: makeHex(palaceIdx * 8 + 1), line: i + 1 })); const dayEl = wuXingOf(bazi.day.gan); const liuQinMap = ben.lines.map((l, i) => { const el = wuXingOf(l.dizhi); let lq = '和'; if (el === dayEl) lq = '比肩'; else { const sheng: Record<string,string> = {木:'火',火:'土',土:'金',金:'水',水:'木'}; const ke: Record<string,string> = {木:'土',火:'金',土:'水',金:'木',水:'火'}; if (sheng[dayEl] === el) lq = '食神'; else if (ke[dayEl] === el) lq = '财爻'; else if (sheng[el] === dayEl) lq = '印爻'; else if (ke[el] === dayEl) lq = '官鬼'; } return { position: i + 1, liuQin: lq, ganZhi: l.tiangan + l.dizhi }; }); const startIdx = ['甲','乙','丙','丁','戊','己','庚','辛','壬','癸'].indexOf(bazi.day.gan); const liuShen = Array.from({ length: 6 }, (_, i) => LIU_SHEN[(startIdx + i) % 6]); let summary = `${ben.fullName}，${ben.palace}宫，世爻${ben.shiPosition}爻`; if (dong.length > 0) summary += `，${dong.join('、')}爻动`; return { ben, bian: makeHex(bianIdx), dong, fuShi, liuShen, liuQinMap, summary: summary + '。' }; }
